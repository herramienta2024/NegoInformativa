import React from "react";
import TitleSection from "../TitleSection";

const Garden = () => {
  return (
    <div className="-mt-[72px] md:-mt-[90px] lg:-mt-[72px]">
      <TitleSection title={`NEO GAREN`} />
    </div>
  );
};

export default Garden;
